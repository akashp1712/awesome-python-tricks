{
  "data": {
    "name": "Asserts that never fail",
    "desc": "Assertion never fails when the first argument is a tupple. It Always evaluates as true",
    "code": ["assert(\"python\" == \"java\", 'Assert Failed')", "# This will always be true and will never fail."]
  }
}
